# Tests Directory

Top-level test layout:

- `tests/demo/`: runnable HA demos and strict-claim wrappers.
- `tests/jepsen/`: Jepsen workload, harness, matrix campaign, and artifacts.

Start with:

1. `tests/demo/README.md` for local run flow.
2. `tests/jepsen/README.md` for workload/checker internals and artifact format.